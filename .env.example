# Base URL for the site (used for metadata, sitemap, Open Graph, canonical).
# Set this in production (e.g. https://markos.app or your deployment URL).
NEXT_PUBLIC_SITE_URL=https://markosh.vercel.app
